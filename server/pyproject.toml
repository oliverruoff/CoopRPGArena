[project]
name = "coop-rpg-arena-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pytest>=8.0.0",
  "pytest-asyncio>=0.24.0",
  "httpx>=0.27.0",
  "websockets>=13.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
